/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
/*--------------------------------------------------------------------
편성정보
--------------------------------------------------------------------*/
//최소 3량이상 편성
switch(FEAT_TRAINS, SELF, sw_DTS_Ctrain_start_stop_3, num_vehs_in_consist >= 3) {
    1: return CB_RESULT_NO_TEXT;
    return string(STR_ERROR_NOT_ENOUGH_LENGTH3);
}

/*--------------------------------------------------------------------
1. 공통객차
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_CPassenge_wagon, (position_in_vehid_chain + 1) <= (num_vehs_in_vehid_chain / 2)) {
    1: set_Cpassenger_wagon;
    set_Cpassenger_wagon_r;
}