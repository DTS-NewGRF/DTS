/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_JMetro_Panto_cargo_subtype_text, cargo_subtype) {
    return string(STR_REFIT_LIVERY_Nishitetsu_8000);
}

switch(FEAT_TRAINS, SELF, sw_JMetro_Panto_wagon, cargo_subtype) {
    set_Nishitetsu_8000_panto_wagon;
}

switch(FEAT_TRAINS, SELF, sw_JMetro_Panto_wagon_capacity_in_custom, cargo_subtype) {
       150;
}
