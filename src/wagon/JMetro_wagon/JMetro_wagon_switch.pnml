/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_JMetro_cargo_subtype_text, cargo_subtype) {
    5: return string(STR_REFIT_LIVERY_Nishitetsu_6000);                    // 니시테츠 6000형
    6: return string(STR_REFIT_LIVERY_Nishitetsu_6050);                    // 니시테츠 6050형
    7: return string(STR_REFIT_LIVERY_Nishitetsu_8000);                    // 니시테츠 8000형
    return string(STR_REFIT_LIVERY_Nishitetsu_9000);
}

switch(FEAT_TRAINS, SELF, sw_JMetro_wagon, cargo_subtype) {
    5: set_Nishitetsu_6000_wagon;
    6: set_Nishitetsu_6050_wagon;
    7: set_Nishitetsu_8000_wagon;
    set_Nishitetsu_9000_wagon;
}

switch(FEAT_TRAINS, SELF, sw_JMetro_wagon_capacity_in_custom, cargo_subtype) {
    5: return 146;
    6: return 146;
    7: return 150;
       134;
}
