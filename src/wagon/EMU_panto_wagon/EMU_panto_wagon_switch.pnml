/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_EMU_Panto_cargo_subtype_text, cargo_subtype) {
    1: return string(STR_REFIT_LIVERY_GTX);          // GTX
    return string(STR_REFIT_LIVERY_Bicheuro);
}

switch(FEAT_TRAINS, SELF, sw_EMU_Panto_wagon, cargo_subtype) {
    1: set_GTX_panto_wagon;
    set_EMU150_panto_wagon;
}

switch(FEAT_TRAINS, SELF, sw_EMU_Panto_wagon_capacity_in_custom, cargo_subtype) {
    1: return 101;
       72;
}
