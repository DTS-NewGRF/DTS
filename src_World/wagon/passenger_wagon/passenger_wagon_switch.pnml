/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
/*--------------------------------------------------------------------
편성정보
--------------------------------------------------------------------*/
//공통객차 텍스트
switch(FEAT_TRAINS, SELF, sw_WPassenger_wagon_cargo_subtype_text, vehicle_type_id) {
    return CB_RESULT_NO_TEXT;
}

/*--------------------------------------------------------------------
1. 공통객차
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_WPassenge_wagon, (position_in_vehid_chain + 1) <= (num_vehs_in_vehid_chain / 2)) {
    1: set_World_wagon;
    set_World_wagon_r;
}